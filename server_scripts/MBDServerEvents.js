// priority: 1000
// ==========================================
// MBD 机器事件处理
// MBD Machine Events Processing
// ==========================================
// 当前代码已注释，用于处理多方块机器的Tick事件
// Currently commented out, used for handling Multiblock Machine Tick events

/*MBDMachineEvents.onTick("mbd2:fire_machine",event=>{
    if(event.getEvent().getMachine().getLevel().getServer().tickCount % 20) return;
    event.getEvent().getMachine().getLevel().runCommandSilent("/say 11111")
})*/